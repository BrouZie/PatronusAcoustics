# Benchmark — fast iteration / regression testing.
# Thin override of default.yaml: short, stationary, no environment.
signal:
  duration: 2.0
  snr_db: 25

drone:
  trajectory: {}
  motion:
    enabled: false

srpphat:
  search:
    resolution_deg: 4.0
  max_freq: 2000.0

environment:
  enabled: false

output:
  save_animation: false
  save_3d_animation: false
  save_figures: false
