# Sweep: gate threshold tuning
base_config: config/default.yaml
sweep:
  srpphat.detection.psr_threshold_db: [2.0, 3.0, 4.0, 6.0, 8.0, 10.0]
  drone.distance: [20, 50, 100]
overrides:
  signal.duration: 5.0
